# URL da API NestJS. Em dev, se omitir, o app usa http://localhost:3000.
# VITE_API_URL=http://localhost:3000

# Produção: aponte para a URL pública do backend (Railway ou subdomínio da API).
# O frontend em produção será servido em https://app.funilariavassoler.com.br
# VITE_API_URL=https://sua-api.up.railway.app
